# Slim airflow runtime — SQLAlchemy<2 mandatory (Airflow 2.9.x incompat 2.0)
sqlalchemy>=1.4,<2.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
requests>=2.31.0
pytz>=2023.3
psycopg2-binary>=2.9.9
geoalchemy2>=0.14.0
shapely>=2.0.0
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0
joblib>=1.3.0
pyproj>=3.6.0
h3>=4.1.0
geopandas>=0.14.0
httpx>=0.25.0
tenacity>=8.2.0
networkx>=3.2.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
boto3>=1.34.0
botocore>=1.34.0
xgboost>=2.0.0
scikit-learn>=1.3.0
# Sprint 15+ (2026-06-19) — pin cryptography<43 pour compat pyOpenSSL 24.x.
# Sans ce pin, cryptography 49+ retire `_lib.GEN_EMAIL` (passé aux bindings Rust),
# ce qui fait crasher l'import de boto3 au moment où Airflow parse les DAGs :
#   File ".../OpenSSL/crypto.py", line 855, in X509Extension
#       _lib.GEN_EMAIL: "email",
#   AttributeError: module 'lib' has no attribute 'GEN_EMAIL'
# Effet concret : `dags/maintenance/silver_archive_to_minio.py` ne se charge
# plus, l'archive silver→MinIO quotidienne échoue. Downgrade stable connu :
# cryptography 42.x (toujours compatible pyOpenSSL 24.0-24.1 + boto3 + botocore).
cryptography>=42.0.0,<43.0.0
# mlflow + evidently pinned compat sqlalchemy 1.4
mlflow>=2.10,<2.16
optuna>=3.5,<4.0
protobuf>=4.25.0,<5.0.0
grpcio>=1.60.0
# Evidently retiré des dépendances Airflow — le drift DAG utilise PSI
# (src/monitoring/psi.py, zéro dépendance). Evidently disponible en
# option pour rapports HTML ponctuels (pip install evidently>=0.7 sur
# le poste local). Voir docs/SPEC_EVIDENTLY_CONFIGURATION.md §8.
