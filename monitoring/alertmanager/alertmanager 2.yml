# =============================================================================
# monitoring/alertmanager/alertmanager.yml — Config Alertmanager
# =============================================================================
# Sprint VPS-3 : route les alertes critiques vers Discord (ou Slack).
<<<<<<< HEAD
# Configuration secrete via .env : ALERT_WEBHOOK_URL.
=======
# Sprint 8+4 (2026-06-12) : receivers webhook désactivés (pas de
# ALERT_WEBHOOK_URL dans .env, sinon container restart-loop).
>>>>>>> origin/main
# =============================================================================

global:
  resolve_timeout: 5m

<<<<<<< HEAD
# Templates
templates:
  - '/etc/alertmanager/templates/*.tmpl'

# Route par défaut
=======
templates:
  - '/etc/alertmanager/templates/*.tmpl'

>>>>>>> origin/main
route:
  group_by: ['alertname', 'component', 'severity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
<<<<<<< HEAD
  receiver: 'discord-default'
  routes:
    # Alertes critiques -> Discord #alerts-critical + Telegram SMS
    - match:
        severity: critical
      receiver: 'discord-critical'
      repeat_interval: 1h
      continue: true
    # Warning -> Discord #alerts (sans spam)
    - match:
        severity: warning
      receiver: 'discord-default'
      repeat_interval: 6h

# Receivers
receivers:
  - name: 'discord-default'
    webhook_configs:
      - url: '${ALERT_WEBHOOK_URL}'
        send_resolved: true
        max_alerts: 10

  - name: 'discord-critical'
    webhook_configs:
      - url: '${ALERT_WEBHOOK_CRITICAL_URL}'
        send_resolved: true
        max_alerts: 0  # pas de limite pour les critiques

# Inhibitions (si DB down, ne pas spammer avec d'autres alertes DB)
=======
  receiver: 'null-receiver'
  routes:
    - match:
        severity: critical
      receiver: 'null-receiver'
      repeat_interval: 1h
      continue: true
    - match:
        severity: warning
      receiver: 'null-receiver'
      repeat_interval: 6h

receivers:
  - name: 'null-receiver'

>>>>>>> origin/main
inhibit_rules:
  - source_match:
      severity: 'critical'
      alertname: 'DatabaseDown'
    target_match:
      component: 'database'
    equal: ['cluster']

  - source_match:
      severity: 'critical'
      alertname: 'NodeDown'
    target_match:
      component: 'vps-host'
    equal: ['cluster']
