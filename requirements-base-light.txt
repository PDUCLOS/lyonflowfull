# =============================================================================
# LyonFlowFull — dépendances communes (light) pour api + streamlit
# =============================================================================
# Sprint P2-ter (2026-06-15) — Allègement des images Docker.
# Ce fichier remplace requirements.txt (99 deps monolithique, image streamlit
# 14.3 GB) par le strict minimum vérifié par grep des imports réels.
#
# AVANT (requirements.txt monolithique) : ~99 deps, image streamlit 14.3 GB
# APRÈS (requirements-base-light.txt) : ~25 deps, image streamlit cible 2-3 GB
#
# Deps RETIRÉES (jamais importées dans api/ ou streamlit/) :
#   - torch, torch-geometric (STGCN fallback gracieux → XGBoost)
#   - geopandas, shapely, pyproj (pas utilisé par les widgets, juste transitif)
#   - h3 (utilisé uniquement dans dags/)
#   - networkx (utilisé uniquement dans src/routing/, pas importé par dashboard)
#   - mlflow (try/import symbolique, pas critique pour le dashboard)
#   - evidently, litestar (jamais importés en runtime)
#   - optuna, openpyxl (Sprint 4, plus utilisé)
#   - weasyprint, reportlab (que dans src/reporting/, jamais appelé par API)
#   - boto3, google-api-* (que dans src/ingestion/, jamais chargé au runtime)
#   - tenacity (que dans src/ingestion/)
#   - polars (que dans dags/)
# =============================================================================

# Config
python-dotenv>=1.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
pyyaml>=6.0
pytz>=2023.3

# HTTP (pour les widgets dashboard + healthchecks API)
httpx>=0.28.0
requests>=2.31.0

# DB
psycopg2-binary>=2.9.9

# Data
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
joblib>=1.3.0

# ML (Sprint P2-bis : xgboost requis pour /api/v1/predict/traffic)
xgboost>=2.0.0,<4.0.0
scikit-learn>=1.3.0,<2.0.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
starlette>=0.36.0
bcrypt>=4.1.0
pyjwt>=2.8.0
prometheus-fastapi-instrumentator>=7.0.0
prometheus-client>=0.20.0

# Streamlit + viz
streamlit>=1.32.0
plotly>=5.18.0
folium>=0.15.0
streamlit-folium>=0.15.0
pydeck>=0.8.0

# Tests (au cas où l'image sert pour CI légère)
pytest>=8.0.0
pytest-asyncio>=0.23.0
