# =============================================================================
# LyonFlow API — FastAPI + auth
# =============================================================================
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
starlette>=0.36.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
bcrypt>=4.1.0
pyjwt>=2.8.0
prometheus-fastapi-instrumentator>=7.0.0
prometheus-client>=0.20.0
psycopg2-binary>=2.9.9
pytest>=9.0.0
pytest-asyncio>=1.0.0
httpx>=0.28.0
# Sprint P2-bis (2026-06-15) — xgboost requis pour l'endpoint
# /api/v1/predict/traffic qui appelle XGBoostSpeedModel.predict().
# Sans ce dep, l'API crash avec ModuleNotFoundError sur tout appel
# a l'endpoint predict (meme si les autres endpoints marchent).
xgboost>=2.0.0
numpy>=1.26.0  # dep transitive de xgboost
pandas>=2.1.0  # dep transitive de xgboost (utilise dans features lookup)
